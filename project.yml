# xcodegen project spec — run: xcodegen generate
name: VRMFacialTracker
options:
  deploymentTarget:
    iOS: "17.0"
    macOS: "13.0"
  xcodeVersion: "15.0"
  groupSortPosition: top
  generateEmptyDirectories: true
  createIntermediateGroups: true

packages:
  GLTFKit2:
    url: https://github.com/warrenm/GLTFKit2
    from: 0.5.0

# ─── iPhone App ───────────────────────────────────────────────────────────────
targets:
  VRMTrackerPhone:
    type: application
    platform: iOS
    sources:
      - path: Shared
        createIntermediateGroups: true
      - path: iOS
        createIntermediateGroups: true
    dependencies:
      - package: GLTFKit2
        product: GLTFKit2
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: jp.chibatech.vrmtracker.phone
      MARKETING_VERSION: "1.0"
      CURRENT_PROJECT_VERSION: "1"
      SWIFT_VERSION: "5.9"
      TARGETED_DEVICE_FAMILY: "1"
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      CODE_SIGN_STYLE: Automatic
      # Xcode の Signing & Capabilities で自分の Team を選ぶか、ここに Team ID を記入:
      # DEVELOPMENT_TEAM: XXXXXXXXXX
    info:
      path: iOS/App/Info.plist
      properties:
        CFBundleDisplayName: "VRM Tracker"
        NSCameraUsageDescription: "ARKit で表情と頭の向きをトラッキングするためにカメラを使用します。"
        NSLocalNetworkUsageDescription: "同じネットワーク上の Mac アプリへトラッキングデータを送信するために使用します。"
        NSBonjourServices:
          - "_vrm-tracker._tcp"
          - "_vrm-tracker._udp"
        # 暗号化の輸出コンプライアンス: 標準/免除暗号のみ → 申告不要
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait

# ─── Mac App ──────────────────────────────────────────────────────────────────
  VRMTrackerMac:
    type: application
    platform: macOS
    sources:
      - path: Shared
        createIntermediateGroups: true
      - path: macOS
        createIntermediateGroups: true
        excludes:
          - Frameworks/**    # don't compile .framework contents as source
    dependencies:
      - package: GLTFKit2
        product: GLTFKit2
      - framework: macOS/Frameworks/Syphon.framework
        embed: true
        codeSign: false
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: jp.chibatech.vrmtracker.mac
      MARKETING_VERSION: "1.0"
      CURRENT_PROJECT_VERSION: "1"
      SWIFT_VERSION: "5.9"
      MACOSX_DEPLOYMENT_TARGET: "13.0"
      ENABLE_APP_SANDBOX: NO
      SWIFT_OBJC_BRIDGING_HEADER: macOS/BridgingHeader.h
      LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
      FRAMEWORK_SEARCH_PATHS: "$(inherited) $(PROJECT_DIR)/macOS/Frameworks"
    info:
      path: macOS/App/Info.plist
      properties:
        NSLocalNetworkUsageDescription: "iPhone からのトラッキングデータ受信に使用します。"
        NSBonjourServices:
          - "_vrm-tracker._tcp"
          - "_vrm-tracker._udp"
        NSPrincipalClass: NSApplication
